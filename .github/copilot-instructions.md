# Copilot Instructions

After making any code changes to this project:

1. Build the app: `bash build.sh`
2. Kill any running instance: `pkill -x SpotifyMenuBar` (ignore if not running)
3. Restart the app: `open SpotifyMenuBar.app`
